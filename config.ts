// Configuration for the Moses (Foxtrot) and Olympus services.
// Please fill in the details below.

export const MOSES_CONFIG = {
  // Hosts (e.g., https://internal-api.phonepe.com)
  mosesHost: "",
  olympusHost: "",

  // Credentials for Olympus
  clientId: "",
  clientSecret: "",

  // Toggle this to TRUE to use the hardcoded mock data (useful if VPN/API is down)
  // Toggle to FALSE to query the real Moses API
  useMockData: true,
};
